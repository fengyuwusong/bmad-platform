# Platform default LLM configuration (server-side only, never sent to the browser)
# Used when the user has not configured a custom LLM provider.

# API key for the default LLM provider (e.g. ZhipuAI GLM)
DEFAULT_API_KEY=your_api_key_here

# Default model name
DEFAULT_MODEL=glm-5
